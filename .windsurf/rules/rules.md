---
trigger: always_on
---

For each coding task, follow these instructions explicitly:

Use clear, descriptive function names and comments.

Implement all tasks modularly and separate into dedicated .py files.

Maintain a clean, consistent, Pythonic style (PEP8 compliance).

Always use robust exception handling (try-except) with clear logging.

Prioritize PySide6 UI responsiveness through QThreadPool and QRunnables.

Use SQLite for simple, no-server-required database management.

File scanning and folder watching must run asynchronously (never block the UI).